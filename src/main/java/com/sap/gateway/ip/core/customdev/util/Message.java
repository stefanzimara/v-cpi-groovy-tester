package com.sap.gateway.ip.core.customdev.util;

import de.cpitester.MessageSupport;

/**
 * Lokaler Nachbau der Message-Klasse aus Groovy Script Version 1.x.
 *
 * Bewusst im Original-Package, damit produktive Scripte ohne jede Aenderung
 * kompiliert und ausgefuehrt werden koennen. Die gesamte Logik steckt in
 * {@link MessageSupport} und wird mit der Version-2-Variante geteilt.
 */
public class Message extends MessageSupport {
}
